module.exports = async (bot, messageReaction,user) => {
    const fs = require(`fs`)
      const { TOKEN, PREFIX, mytime, activity, ownerid, clientID, by, BOTA} = require('../modules/config');
      const { ds , mcs } = require('../usage/staff');







}